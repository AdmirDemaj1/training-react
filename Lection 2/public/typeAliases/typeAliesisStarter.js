"use strict";
const logoDetajet = (id, item) => {
    console.log(`${item} ka id ${id} `);
};
const helloFunction = (user) => {
    console.log(`${user.name} ju thote hello `);
};
const goodbyeFunction = (user) => {
    console.log(`${user.name} ju thote goodbye `);
};
